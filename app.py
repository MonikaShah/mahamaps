# This project is a Django application only.
# Run the site with Django:
#   python manage.py runserver
# For production, use the WSGI application in climateportal.wsgi (e.g. gunicorn climateportal.wsgi:application).
